$(document).ready(function () {
  // Hide the content initially
  $('.oferta-especial1-content').hide();

  // Attach click event handler to the toggle button
  $('.oferta-especial1-toggle').click(function () {
    // Toggle the content display
    $('.oferta-especial1-content').slideToggle();
  });

  // Hide the content initially
  $('.oferta-especial2-content').hide();

  // Attach click event handler to the toggle button
  $('.oferta-especial2-toggle').click(function () {
    // Toggle the content display
    $('.oferta-especial2-content').slideToggle();
  });

  // Hide the content initially
  $('.oferta-especial3-content').hide();

  // Attach click event handler to the toggle button
  $('.oferta-especial3-toggle').click(function () {
    // Toggle the content display
    $('.oferta-especial3-content').slideToggle();
  });

  // Hide the content initially
  $('.raciones-content').hide();

  // Attach click event handler to the toggle button
  $('.raciones-toggle').click(function () {
    // Toggle the content display
    $('.raciones-content').slideToggle();
  });

  // Hide the content initially
  $('.pasta-content').hide();

  // Attach click event handler to the toggle button
  $('.pasta-toggle').click(function () {
    // Toggle the content display
    $('.pasta-content').slideToggle();
  });

  // Hide the content initially
  $('.tostadas-content').hide();

  // Attach click event handler to the toggle button
  $('.tostadas-toggle').click(function () {
    // Toggle the content display
    $('.tostadas-content').slideToggle();
  });

  // Hide the content initially
  $('.platos-de-mi-casa-content').hide();

  // Attach click event handler to the toggle button
  $('.platos-de-mi-casa-toggle').click(function () {
    // Toggle the content display
    $('.platos-de-mi-casa-content').slideToggle();
  });

  // Hide the content initially
  $('.hamburguesas-content').hide();

  // Attach click event handler to the toggle button
  $('.hamburguesas-toggle').click(function () {
    // Toggle the content display
    $('.hamburguesas-content').slideToggle();
  });

  // Hide the content initially
  $('.risoto-content').hide();

  // Attach click event handler to the toggle button
  $('.risoto-toggle').click(function () {
    // Toggle the content display
    $('.risoto-content').slideToggle();
  });

  // Hide the content initially
  $('.bocadillos-content').hide();

  // Attach click event handler to the toggle button
  $('.bocadillos-toggle').click(function () {
    // Toggle the content display
    $('.bocadillos-content').slideToggle();
  });

  // Hide the content initially
  $('.postres-content').hide();

  // Attach click event handler to the toggle button
  $('.postres-toggle').click(function () {
    // Toggle the content display
    $('.postres-content').slideToggle();
  });

  // Hide the content initially
  $('.té-y-café-content').hide();

  // Attach click event handler to the toggle button
  $('.té-y-café-toggle').click(function () {
    // Toggle the content display
    $('.té-y-café-content').slideToggle();
  });

  // Hide the content initially
  $('.zumos-y-batidos-content').hide();

  // Attach click event handler to the toggle button
  $('.zumos-y-batidos-toggle').click(function () {
    // Toggle the content display
    $('.zumos-y-batidos-content').slideToggle();
  });

  // Hide the content initially
  $('.refrescos-content').hide();

  // Attach click event handler to the toggle button
  $('.refrescos-toggle').click(function () {
    // Toggle the content display
    $('.refrescos-content').slideToggle();
  });

  // Hide the content initially
  $('.vinos-content').hide();

  // Attach click event handler to the toggle button
  $('.vinos-toggle').click(function () {
    // Toggle the content display
    $('.vinos-content').slideToggle();
  });

  // Hide the content initially
  $('.cervezas-content').hide();

  // Attach click event handler to the toggle button
  $('.cervezas-toggle').click(function () {
    // Toggle the content display
    $('.cervezas-content').slideToggle();
  });

  // Hide the content initially
  $('.cocktails-content').hide();

  // Attach click event handler to the toggle button
  $('.cocktails-toggle').click(function () {
    // Toggle the content display
    $('.cocktails-content').slideToggle();
  });

  // Hide the content initially
  $('.chupitos-content').hide();

  // Attach click event handler to the toggle button
  $('.chupitos-toggle').click(function () {
    // Toggle the content display
    $('.chupitos-content').slideToggle();
  });

  // Hide the content initially
  $('.para-picar-content').hide();

  // Attach click event handler to the toggle button
  $('.para-picar-toggle').click(function () {
    // Toggle the content display
    $('.para-picar-content').slideToggle();
  });

  // Hide the content initially
  $('.menu-infantil-content').hide();

  // Attach click event handler to the toggle button
  $('.menu-infantil-toggle').click(function () {
    // Toggle the content display
    $('.menu-infantil-content').slideToggle();
  });






});

